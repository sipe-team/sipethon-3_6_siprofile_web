import React from 'react';
import { TextField } from '@mui/material';

type CreateCardFormProps = {};

const CreateCardForm = ({}: CreateCardFormProps) => {
  return <div>CreateCardForm</div>;
};

export default CreateCardForm;
